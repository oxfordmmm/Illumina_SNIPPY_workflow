process {
    withLabel: 'snippy' {
        conda = "${projectDir}/envs/snippy.yaml"
    }
    withLabel: 'blast' {
        conda = "${projectDir}/envs/blast.yaml"
    }
    withLabel: 'snpdist' {
        conda = "${projectDir}/envs/blast.yaml"
    }
}